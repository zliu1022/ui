#!/bin/bash

#!/bin/bash

# 清空 out.txt 文件
> out.txt

cat <<EOF > out.txt
围棋死活做题界面程序如下，已经可以正常工作，包括显示表格q中的围棋题目，可以根据题目中棋子的范围动态只显示局部棋盘，以及通过索引表格q_search搜索相似的围棋棋形,包括动态显示相似棋形匹配的数量

python编程，显示题目的功能不改动，改进搜索题目的功能
1. 点击搜索棋盘后，立即在q_search索引数据库中进行棋形匹配并按照min_pp_list数组的size排序，最短的排在前面,返回前30个
2. 搜索棋盘界面的下面已经有按钮和提示标签，需要在它们的右边，分3列显示匹配到的前30个题目，要有滚动条, 显示的题目图形小一点，是搜索棋盘尺寸的一半
3. 实时匹配棋形的显示格式和主功能显示的格式一致，中间是棋形，下面的Q-publicid，然后是题目类型和难度等级
4. 显示的时候，需要模仿game.py里面的load_problem，先获取全部棋子的坐标范围，然后change_size，这样才能显示局部棋盘

不需要导出全部完整的代码，只需要指出修改哪个文件的哪个部分即可
EOF

# 遍历所有的 .py 文件
for file in *.py; do
    # 输出文件名
    echo "${file}：" >> out.txt

    # 输出文件内容
    echo '```python' >> out.txt
    cat "$file" >> out.txt
    echo '```' >> out.txt

    echo '' >> out.txt  # 添加空行分隔
done

